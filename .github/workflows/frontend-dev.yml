name: Frontend Dev CI

on:
  pull_request:
    branches:
      - dev
    paths:
      - "frontend/**"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run frontend dev tests
        run: |
          echo "TODO: Frontend dev tests"